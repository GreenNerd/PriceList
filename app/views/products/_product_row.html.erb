<div class="col-xs-6 col-md-3 col-sm-12">
  <div class="product">
    <a href=<%= product_path(product.id) %> id="imagelink">
      <%= image_tag(qiniu_image_url(product.image_url, :index), alt: "1", class: "thumb") %>
    </a>
    <div class="items">
      <div class="pname">
        <%= product.name %>
      </div>
      <div class="pprice">
        <%= number_to_currency product.price %>
        <div class="addtocart">
          <% if StockKeepingUnit.where("product_id=#{product.id}").count > 1 %>
            <%= button_to product_path(product.id), :method => "get" do %>
              <i class="fa fa-shopping-cart" aria-hidden="true"></i>
            <% end %>
          <% elsif StockKeepingUnit.where("product_id=#{product.id}").count == 1 %>
            <button id="addtocart1">
              <i class="fa fa-shopping-cart" aria-hidden="true"></i>
            </button>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
