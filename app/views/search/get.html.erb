<h3 class="text-center">Products for Search</h3>

<div class="row">
  <div class="col-xs-6 col-xs-offset-3">
    <% if !@products.empty? %>
      <% @products.each do |product| %>
        <%= render "product_row", product: product, order_item: @order_item %>
      <% end %>
    <% else %>
      render html: "no results"
      <% render html: "no results" %>
    <% end %>
  </div>
</div>
